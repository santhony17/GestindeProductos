<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.gestiondeproductos">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GestiónDeProductos"
        tools:targetApi="31">

        <!-- Configuración opcional para Firebase (si no usas Firebase, puedes eliminar estas líneas) -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorPrimary" />
        <meta-data
            android:name="com.google.firebase.analytics.analytics_collection_enabled"
            android:value="true" />

        <!-- Actividad principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad para agregar productos -->
        <activity
            android:name=".AgregarProductoActivity"
            android:exported="true" />

        <!-- Actividad para buscar productos -->
        <activity
            android:name=".BuscarProductoActivity"
            android:exported="true" />

        <!-- Actividad para ver productos -->
        <activity
            android:name=".VerProductosActivity"
            android:exported="true" />

    </application>

</manifest>


